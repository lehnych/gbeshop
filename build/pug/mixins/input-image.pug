mixin inputImage(inputId, inputName, inputSrc, inputAlt, inputWidth, inputHeight, inputClass)
	.form-group
		input(
			type='image'
			id=inputId
			name=inputName
			src=inputSrc
			alt=inputAlt
			width=inputWidth
			height=inputHeight,
			class=inputClass
		)
